import { ScanSearch } from 'lucide-react';

export default function DetectPage() {
  // This is re-exported from the full detection page
  // Keeping this as a placeholder in case the dynamic import is needed
  return null;
}
